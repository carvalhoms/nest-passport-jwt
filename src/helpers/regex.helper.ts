// Regex generate: https://ihateregex.io
// >> https://ihateregex.io/expr/password
const password =
  /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$ %^&*-]).{8,}$/;

export const RegExHelper = {
  password,
};
